# Create Project
npx express-generator training --no-view

# Optional: ES6
var express = require('express');
import express from 'express';

# Optional: auto reload
npm i -s nodemon rimraf

# Require module
npm i -s dotenv cors mongoose mong
